# Storage Directory

This directory is used for JSON file storage when APCu is not available.

**Note:** The `chat.json` file will be created automatically when needed.

Do not commit `chat.json` as it contains temporary session data.
